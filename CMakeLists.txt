project(ledger-secure-sdk)

cmake_minimum_required(VERSION 3.10)

set(CMAKE_EXPORT_COMPILE_COMMANDS ON)

message(FATAL_ERROR "CMake is not supported for ledger application development yet. Use Makefile instead.")
